Generate a new tutors course - to be called tutors-module-by-dept. This should be structured in a simuliar manner to tutors-master, except:

- the root page should contain 2 units
- Unit 1 should contain three topics for "Programmes", "Clusters", "All Modules", but only for "Computing and Maths" department
- Unit 2 should be the same, but be for the "Science" department

Use the same .env file settings as for tutors-master.

The generation script should be called "generate-by-dept"