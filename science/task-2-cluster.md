
In tutors-master, move all of the topics into a topic-01-cluster folder. This topic should be titled "All modules by Cluster".

Create another topic at the top level titles "All Modules", this should list all modules alphabetically as web learning objects referencing the master copy which is retained in the topic-01-cluster topic, (modeled on the progrgrammes / topic / unit / web objects in the computing catalogue). The weburl should take account of the env file values TUTORS_COURSE_ID in composing the link. Preserve the icon used by the module in the web learning object